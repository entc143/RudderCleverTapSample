<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required by the RudderStack SDK / CleverTap integration -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_app_icon_pink"
        android:roundIcon="@drawable/ic_app_icon_pink"
        android:logo="@drawable/ic_app_icon_pink"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RudderCleverTapSample">

        <meta-data
            android:name="CLEVERTAP_NOTIFICATION_ICON"
            android:value="ic_app_icon_pink"/>

        <!--
            CLEVERTAP_ACCOUNT_ID / CLEVERTAP_TOKEN meta-data are optional here: the RudderStack
            device-mode integration pulls the Account ID, Account Token, and Region from the
            CleverTap destination configured in your RudderStack dashboard and initializes the
            CleverTap SDK for you.
        -->
        <!--
        <meta-data android:name="CLEVERTAP_ACCOUNT_ID" android:value="YOUR_CLEVERTAP_ACCOUNT_ID" />
        <meta-data android:name="CLEVERTAP_TOKEN" android:value="YOUR_CLEVERTAP_TOKEN" />
        -->

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent Filter for Custom Deep Links -->
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accepts rudderclevertap://... -->
                <data android:scheme="rudderclevertap" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".MainActivityPink"
            android:targetActivity=".MainActivity"
            android:icon="@drawable/ic_app_icon_pink"
            android:roundIcon="@drawable/ic_app_icon_pink"
            android:label="@string/app_name"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".ThankYouActivity"
            android:exported="false" />

        <activity
            android:name=".WelcomeActivity"
            android:exported="false" />

        <service
            android:name="com.clevertap.android.sdk.pushnotification.fcm.FcmMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <meta-data
            android:name="CLEVERTAP_BACKGROUND_SYNC"
            android:value="1"/>

    </application>

</manifest>
